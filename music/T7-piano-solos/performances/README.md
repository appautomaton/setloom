# Retained piano performances

Each piece keeps its original transcription (`notes.raw.mid`) and the cleaned
performance (`notes.clean.mid`). These files were moved without changing their
contents. Published masters are under `../published/`; scratch renders belong
in project `tmp/`. This organization does not reopen the completed piano work.
